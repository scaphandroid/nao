{% block body %}
    id"username"email"prenom"nom"profession"motivation"date_inscription"type_compte
    {% for row in data %}
        {{ row.id }}"{{ row.username }}"{{ row.email }}"{{ row.prenom }}"{{ row.nom }}"{{ row.profession }}"{{ row.motivation }}"{{ row.dateinscription|date('d-m-Y') }}"{{ row.typecompte }}
    {% endfor %}
{% endblock %}
